# PostCSS / autoprefixer install artifacts (Docsy CSS pipeline).
node_modules/
package-lock.json

# Hugo build output.
public/
resources/

# Hugo module cache.
.hugo_build.lock

# go.sum is generated by `hugo mod get`. Allow it to be committed
# alongside go.mod (Hugo modules behave like Go modules), but it is
# auto-managed — do not edit by hand.
